export enum ToolStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  ARCHIVED = 'archived',
}

export enum ToolVersionStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
}

export enum ToolType {
  SEARCH = 'search',
  BROWSER = 'browser',
  IMAGE_GENERATION = 'image_generation',
  STORAGE = 'storage',
  HTTP = 'http',
  CUSTOM = 'custom',
}
